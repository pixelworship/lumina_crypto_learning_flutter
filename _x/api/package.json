{
  "name": "lumina-events-api",
  "version": "0.1.0",
  "private": true,
  "description": "Mock crypto news/events API for the Lumina Flutter app — deterministic, seeded by (symbol, time) so chart markers stay stable across reloads.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3",
    "dotenv": "^17.4.2",
    "express": "^4.21.0"
  }
}
